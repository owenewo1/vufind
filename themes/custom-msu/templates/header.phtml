<?php $account = $this->auth()->getManager(); ?>
<header class="navbar navbar-expand-lg navbar-dark bg-primary py-3">
  <div class="container-fluid">
    <!-- Branding / Logo -->
    <a class="navbar-brand d-flex align-items-center" href="<?=$this->url('home')?>">
      <img src="/themes/custom-msu/images/msu-logo.png" alt="MSU Logo" height="40" class="me-2">
      <span class="fw-bold text-white">MSU Library Search</span>
    </a>

    <!-- Mobile menu button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#msuNavbar"
            aria-controls="msuNavbar" aria-expanded="false" aria-label="<?=$this->transEscAttr('Toggle navigation')?>">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Nav + Search + User -->
    <div class="collapse navbar-collapse" id="msuNavbar">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
        <li class="nav-item"><a class="nav-link" href="/About">About</a></li>
        <li class="nav-item"><a class="nav-link" href="/Help">Help</a></li>
      </ul>

      <!-- Account / Login Links -->
      <ul class="navbar-nav mb-2 mb-lg-0">
        <?php if (is_object($account) && $account->loginEnabled()): ?>
          <?php if ($account->getIdentity()): ?>
            <li class="nav-item">
              <a class="nav-link" href="<?=$this->url('myresearch-home')?>"><?=$this->transEsc('Your Account')?></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<?=$this->url('myresearch-logout')?>"><?=$this->transEsc('Log Out')?></a>
            </li>
          <?php else: ?>
            <li class="nav-item">
              <a class="nav-link" href="<?=$this->url('myresearch-userlogin')?>"><?=$this->transEsc('Login')?></a>
            </li>
          <?php endif; ?>
        <?php endif; ?>
      </ul>

      <!-- Search Box -->
      <?php if ($this->layout()->searchbox !== false): ?>
        <div class="d-flex ms-lg-3" role="search">
          <?=$this->layout()->searchbox ?>
        </div>
      <?php endif; ?>
    </div>
  </div>
</header>
